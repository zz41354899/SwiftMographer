{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const path=require('node:path'); const root=process.env.CODEX_PLUGIN_ROOT || process.env.CLAUDE_PLUGIN_ROOT || process.cwd(); require(path.join(root, 'scripts', 'validate-storyboard.js'));\"",
            "timeout": 10,
            "statusMessage": "Validating storyboard artifacts"
          }
        ]
      }
    ]
  }
}
